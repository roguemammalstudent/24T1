using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Doorway : MonoBehaviour
{

    // These are so named due to my wretched brain and my fear of conflicting names.
    // teleporter refers to the doorway, the object that is causing the teleportation. 
    // scottyTo is the location the Player is sent to ...
    // ... scottyTo as in "Beam me Scotty to...", little Star Trek reference for all you Trekkies out there :)
    public GameObject teleporter;
    public GameObject scottyTo;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
